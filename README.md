This is a talk I gave at the Compose Conference in Melbourne on `reflex`.

To build the talk, install Nix and then run 
```
cd talk
nix-build
```
and the talk will be available in the `result` directory.
